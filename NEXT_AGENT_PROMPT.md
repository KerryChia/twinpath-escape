# 下一阶段开发 Agent Prompt

将本文件的完整内容交给新的开发 Agent。仓库地址：

https://github.com/KerryChia/twinpath-escape

---

你是一名资深 Python/Pygame 游戏开发、搜索算法和关卡设计工程师。请直接在 `KerryChia/twinpath-escape` 仓库工作，不要停留在口头建议。本阶段有两个同等重要、必须都完成的核心目标：

1. 增加真正的“人机协作模式”：玩家操作一个角色，AI 使用搜索算法控制另一个角色。
2. 补全当前最后一关及结局流程，使最后关卡成为可以正常游玩、合作解谜、完成并进入结尾的完整关卡。

## 一、开始前要求

1. 先检查仓库、README、NOTICE、许可证和现有测试。
2. 运行当前测试和 `run_windows.bat --check`，确认基线可用。
3. 实际阅读这些关键文件，而不是只看 README：
   - `core/player.py`
   - `core/scenes/gameplay.py`
   - `core/scenes/base_gameplay.py`
   - `core/config/levels.py`
   - `core/map_loader.py`
   - `core/doors.py`
   - `core/portal.py`
   - `core/moving_platform.py`
   - `core/interactable.py`
   - `assets/tiled/level_002.tmx`
4. 当前项目已经完成简体中文化。所有新增玩家可见文本必须进入 `assets/locales/en.json` 和 `assets/locales/zh_CN.json`，不能重新散落硬编码英文。
5. 不得删除上游 MIT 许可证、`NOTICE.md`、`THIRD_PARTY_NOTICES.txt` 或现有素材许可证。
6. 不要引入来源不明或禁止原文件再分发的素材。
7. 不要破坏原有“本地双人”模式；新增人机模式必须与其并存。

## 二、目标一：人类 + 搜索算法 AI 模式

### 2.1 用户体验

在主菜单新增明确的中文按钮，例如：

- `本地双人`
- `人机协作`
- 原有其他菜单项

进入“人机协作”后：

- 人类默认控制玩家 1（小绿）。
- AI 控制玩家 2（小橙）。
- 可以在开始前选择搜索算法：至少支持 **BFS、DFS、A***。
- 提供“自动/关闭调试信息”选项或快捷键。
- UI 显示当前算法、当前目标、已展开节点数、当前规划路径长度、重新规划次数等简洁指标，便于课程演示。
- 所有新增 UI 都要支持 800×600、1280×720、1920×1080。

### 2.2 算法真实性要求

AI 必须真实调用仓库中自行实现的 BFS、DFS 和 A*，不能用以下方式伪装：

- 直接把角色传送到目标坐标。
- 写死完整按键脚本或关卡路线，然后仅把名称标成 BFS/A*。
- 直接读取未来状态或绕过碰撞。
- 使用第三方 pathfinding 库代替核心搜索实现。

搜索算法应放在独立、可测试的模块，例如：

```text
core/ai/
├─ actions.py
├─ controller.py
├─ graph.py
├─ search.py
├─ observation.py
└─ debug_overlay.py
```

算法至少应返回可解释的节点路径与统计信息。

### 2.3 先抽象动作接口

不要让 AI 伪造全局键盘事件。把角色控制抽象为统一动作：

```python
@dataclass(frozen=True)
class Action:
    left: bool = False
    right: bool = False
    jump: bool = False
    down: bool = False
```

建议提供：

```python
class ActionProvider(Protocol):
    def get_action(self, observation: Observation) -> Action: ...
```

- 本地双人模式：两个 `KeyboardActionProvider`。
- 人机模式：人类使用 `KeyboardActionProvider`，AI 使用 `SearchActionProvider`。
- 两种模式必须复用相同 `Player` 物理和碰撞。
- AI 只能输出 left/right/jump/down，不允许直接写角色 `rect`、位置或速度。

尽量把模拟推进与渲染分开，并使用稳定的时间步或限制最大 `dt`，保证测试和重新规划结果可重复。

### 2.4 平台图和搜索状态

这是平台游戏，不应简单把屏幕像素当普通四邻域迷宫。请从 TMX 和运行时实体提取可用于搜索的平台图：

- 节点：可站立平台区段、楼梯、移动平台停靠点、压力板、门两侧、出口区域等。
- 边：步行、跳跃、下落、爬楼梯、搭乘移动平台，以及门开启后才存在的条件边。
- 边应记录执行动作、方向、预计耗时或代价。
- A* 必须使用合理启发函数；说明为何不会高估，或明确采用加权 A* 并标注。
- DFS、BFS、A* 使用同一图和同一目标接口，便于对比。

Observation 至少包含：

- 两名玩家的位置、速度、落地状态和当前平台；
- 静态平台、危险区、水、熔岩；
- 普通门、协作门和压力板状态；
- 移动平台位置/方向；
- portal/结局目标状态；
- 当前关卡 ID。

### 2.5 目标选择与协作行为

AI 不只需要“走到终点”，还要能参与合作机关。实现清晰、有限且可解释的高层目标选择：

- 到达指定压力板并等待；
- 穿过已打开的门；
- 在需要时等待人类通过；
- 与人类汇合；
- 到达自己的出口/portal；
- 发现路径无效、角色卡住或机关变化时重新规划。

允许使用一个小型协作任务调度器来决定“搜索的目标是什么”，但每个移动路径必须由选择的 BFS/DFS/A* 实际求出。不要制作庞大、难以解释的行为树。

实现卡住检测与恢复：

- 若若干秒位置没有有效变化，重新定位当前节点并重新规划；
- 路线失败时有限次尝试替代目标或复位；
- 不能无限左右抖动、连续无效跳跃或永久卡死在墙边。

## 三、目标二：补完整最后关卡和结局

### 3.1 先审查当前结尾

当前正式关卡为：

- `tutorial_001`
- `level_001`
- `level_002`

`level_002` 是未完整收尾的最后关卡，已有门、协作门、移动平台、水、熔岩、破碎平台和特殊结局触发。请先实际运行和阅读 TMX/代码，明确未完成部分，然后选择最小风险方案：

- 优先补全 `level_002`，使它成为完整终章；
- 如果现有结构确实不适合作完整终章，可以完成 `level_002` 后新增 `final_001.tmx`，但不能留下原关卡断裂或不可完成。

### 3.2 最终关卡设计要求

最后关卡必须：

- 从正常关卡流程进入，不依赖命令行跳关。
- 两个角色都能从出生点开始正常完成。
- 至少包含一次真正的人机协作：AI 需要踩住/操作一个机关，人类借此通过；随后人类也需要为 AI 创造通过条件，或双方在不同路线完成联动。
- 合理使用已有系统：普通压力板、协作门、移动平台、破碎平台、水/熔岩、楼梯和双人出口；不要为了关卡一次性需求写大量特例。
- 对 BFS、DFS、A* 都存在可执行路线；算法表现可以不同，但都不能因关卡设计必然失败。
- 不允许只有一条极度苛刻的像素级跳跃路线。
- 设置检查点或可靠复位，失败后不应重打整个长关卡。
- 关卡完成条件必须明确，两个角色都满足条件后进入完整结尾字幕/结束画面，再安全返回主菜单或关卡选择。
- 不再使用含糊的越界 `Limit` 触发来假装结束；应有明确、可测试的完成状态。

所有新地图必须是原创布局，不复制商业游戏关卡。

### 3.3 关卡数据化

若当前“最近压力板关联最近门”的逻辑无法可靠表达终章机关，请在 TMX object/tile properties 中加入稳定 ID，例如：

```text
trigger_id
controls
required_player
mode = hold | latch
```

但必须保持旧关卡兼容。一个触发器控制多个目标、持续踩住和一次性锁存应有清楚定义与测试。

## 四、必须修复的既有问题

在不扩大范围的前提下，若这些问题会阻碍 AI 或终章，请修复并加回归测试：

1. 移动平台未可靠携带站立玩家。
2. 长坠落死亡计时可能在判定前被重置。
3. 门关闭时可能把玩家卡在碰撞体内。
4. README/启动参数和实际行为必须一致。
5. 最后一关完成流程不是标准可测试状态。

## 五、测试与验证

至少新增以下自动测试：

### 搜索算法单元测试

- BFS 在无权图得到最少边路径。
- DFS 返回合法路径并正确处理无解。
- A* 在给定代价图上得到最低代价路径。
- 三种算法处理起点等于终点、无解、循环图和动态封路。
- 搜索统计数据正确重置。

### 平台图测试

- 从测试 TMX 提取预期节点与边。
- 不生成穿墙、穿熔岩或明显不可达跳跃边。
- 门关闭/打开时条件边状态正确。
- 移动平台连接只在合理状态下可用。

### 控制器测试

- AI 只通过 Action 接口控制玩家。
- 人类输入仍然有效。
- 本地双人模式没有行为回归。
- AI 卡住后会重新规划，不无限抖动。

### 关卡集成测试

- 教程、第一关和最后关卡均能加载。
- 两个出生点、目标和机关引用完整。
- 人机模式可在至少一个简化测试关卡中自动完成协作流程。
- 最后关卡存在一条经关卡验证器确认的合法合作路线。
- 完成最后关卡后进入结局状态。

### 实际运行验证

必须从源码真实启动，不只跑静态测试：

1. 打开主菜单。
2. 进入“人机协作”。
3. 分别选择 BFS、DFS、A* 运行。
4. 人类移动，确认 AI 会根据机关和人类状态重新规划。
5. 至少完整游玩最后关卡一次。
6. 验证结尾字幕和返回流程。
7. 检查 800×600、1280×720、1920×1080。

记录关键截图和运行日志，但不要提交无关桌面信息。

## 六、文档与交付

完成后更新：

- `README.md`：人机模式入口、算法选择、操作、终章说明。
- 新建 `AI_ARCHITECTURE.md`：动作接口、Observation、平台图、BFS/DFS/A*、重规划和限制。
- 新建或更新 `LEVEL_DESIGN.md`：最后关卡机关关系和通过流程。
- 新建 `CHANGELOG.md`：本阶段修改。
- 如有兼容性修复，写清楚原因和测试。

最终汇报必须明确：

- 修改了哪些文件；
- AI 如何获取状态、搜索并输出动作；
- BFS、DFS、A* 各自是否通过测试；
- 最后关卡如何合作、如何完成；
- 哪些旧功能保持不变；
- 实际运行了哪些流程；
- 仍有哪些已知问题。

## 七、明确禁止

- 不要删除本地双人模式。
- 不要把 AI 写成传送、坐标作弊或固定录像脚本。
- 不要只实现寻路演示而不接入真实角色。
- 不要只画一张新地图却不验证可完成性。
- 不要为了赶进度删除碰撞、危险区或机关。
- 不要加入在线大模型/API 依赖；AI 必须是本地可解释搜索算法。
- 不要上传密钥、个人路径、虚拟环境、缓存、构建产物或来源不明素材。
- 未经明确要求不要推送到远程仓库；先完成本地实现、测试和汇报。

现在开始：先建立基线测试和动作抽象，再做平台图与三种搜索，随后接入人机模式，最后补全并完整验证终章。不要停在设计文档阶段。
